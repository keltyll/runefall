[package]
name = "runefall"
version = "0.1.0"
edition = "2021"
description = "Ultra-light runic terminal screensaver"

[dependencies]
crossterm = "0.28"
rand = "0.8"

[profile.release]
opt-level = 3
lto = true
strip = true
